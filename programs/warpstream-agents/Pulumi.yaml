name: warpstream-agents
description: A pulumi program to deploy warpstream agents in GCP
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
